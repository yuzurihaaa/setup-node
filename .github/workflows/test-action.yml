name: Test action

on:
  push:
    branches:
      - test/run-action
  workflow_dispatch:

jobs:
  test-setup-node:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['20.x', '25.x', '26.x']
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: ./
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Verify Node.js version
        run: node --version

      - name: Verify npm version
        run: npm --version
